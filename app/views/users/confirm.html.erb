<div class="page-header">
  <h1>Welcome, <%= request.env['omniauth.auth'].info.name %></h1>
</div>

<p>You are creating a <b>new account</b>. Are you sure this is what you want to do? Adding an OpenID to an existing account requires you to login first.</p>
<p>Check the details below, and confirm</p>

<form id="new-user-info">
  <input id="name" type="text" value="<%= request.env['omniauth.auth'].info.name %>" />
  <input id="email" type="email" value="<%= request.env['omniauth.auth'].info.email %>" />
</form>