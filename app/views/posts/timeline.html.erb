<div class="page-header">
  <h1>Timeline for <%= @post.class.to_s.downcase %></h1>
</div>

<% @timeline_events.each do |event| %>
  <%= render partial: %(posts/timeline/#{event.action}), locals: { event: event } %>
<% end %>