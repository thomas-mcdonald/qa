<div class="post-diff">
  <% if event.question? %>
    <h2><%= event.title_diff %></h2>
  <% end %>

  <%= event.body_diff %>

  <% if event.question? %>
    <%= event.tags_diff %>
  <% end %>
</div>
