<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to "QA", root_url, :class => :brand %>
          <ul class="nav">
            <li><%= link_to "Questions", root_url %></li>
            <li><%= link_to "Tags", tags_url %></li>
            <li><%= link_to "Users", users_url %></li>
            <li><%= link_to "Badges", badges_url %></li>
            <li><%= link_to "Ask Question", new_question_url %></li>
          </ul>
          <ul class="nav pull-right">
            <% if logged_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle"><%= current_user.name %> <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Your Profile", user_url(current_user) %></li>
                  <li><%= link_to "Edit your details", edit_current_user_url %></li>
                  <li class="divider"></li>
                  <li><%= link_to "Log out", logout_url, 'data-skip-pjax' => 1 %></li>
                </ul>
              </li>
              <li><p><%= current_user.reputation %></p></li>
              <% if @notifications %>
                <li><a class="label notification" id="notification-toggle" title="notifications" href="#"><%= @notifications.size %></a></li>
              <% end %>
              <% if current_user.moderator? and @flag_count != 0 %>
                <li><%= link_to @flag_count, flags_url, :class => "label warning", :id => "flag-count", :title => "active flags" %></li>
              <% end %>
           <% else %>
             <li><%= link_to "Register", signup_url %></li>
             <li><%= link_to "Log in", login_url %></li>
           <% end %>
         </ul>
        </div>
      </div>
    </div>
    <div id="notification-centre">
      <div class="container">
        <h2>Notifications</h2>
        <ul>
          <% if !@notifications.blank? %>
            <% @notifications.each do |n| %>
              <li class="notification"><%= send n.token, n %></li>
            <% end %>
          <% else %>
            <p>No active notifications</p>
          <% end %>
        </ul>
      </div>
    </div>
    <div class="container" id="page-root">
      <% flash.each do |name, msg| %>
        <div class="alert alert-<%= name %>">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </div>
    <footer class="footer">
      <div class="container">
        <p>powered by <a href="http://github.com/thomas-mcdonald/qa">QA</a></p>
      </div>
    </footer>
  </body>
</html>
